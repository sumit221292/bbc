{
  "name": "btc-e2e",
  "version": "1.0.0",
  "description": "Playwright E2E tests for the BTC trading app",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test --workers=1",
    "test:headed": "playwright test --headed --workers=1",
    "test:local": "BASE_URL=http://localhost:5173 playwright test --headed --workers=1",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
